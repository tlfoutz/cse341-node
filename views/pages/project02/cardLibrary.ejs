<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>(View)10 Prove : Assignment - Milestone 1 - End to end</title>
	</head>
	<body>
		<h1>Hearthstone Collectible Card Libary</h1>
		<form action="/cardLibrary" method="GET">
			<table>
				<tr>
					<th>Name</th>
					<th>Class</th>
					<th>Mana</th>
					<th>Attack</th>
					<th>Health</th>
					<th>Card Type</th>
					<th>Rarity</th>
					<th>Minion Type</th>
				</tr>
				<% for (var i =0; i < cards.length;  i++ ) { %>
					<tr>
						<td><%= cards[i].name %></td>
						<td><%= cards[i].classid %></td>
						<td><%= cards[i].manacost %></td>
						<td><%= cards[i].attack %></td>
						<td><%= cards[i].health %></td>
						<td><%= cards[i].cardtypeid %></td>
						<td><%= cards[i].rarityid %></td>
						<td><%= cards[i].miniontypeid %></td>
					</tr>
				<% } %>		
			</table>
		<form>
	</body>
</html>